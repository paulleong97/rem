const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    return message.channel.send(`I'm loyal to pol and pol alone! :blue_heart: `);
}

  module.exports.help = {
    name: "single?"
  }
